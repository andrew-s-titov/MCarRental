server.port: 8888

spring:
  application.name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:./config-server/properties
  profiles:
    active: native